//
//  drawingsButton.swift
//  JackNightsky-task8
//
//  Created by Jack on 18.07.21.
//

import UIKit

class DrawingsButton: AppRegularButton {
    // пока ниче не работает, но класс кнопки на swift xD не прикалупатся
//    enum CurrentStatement : Int {
//        case ready, active
//    };
//    
//    public var currentState: CurrentStatement = .ready
//    
//    func setCurrentState(currentState: CurrentStatement) {
//        switch (currentState) {
//        case .ready:
//            self.currentState = .ready
//            self.layer.shadowColor = UIColor.rsLightGreenSea.withAlphaComponent(1).cgColor;
//            self.layer.shadowRadius  = 2;
//            break;
//        case .active:
//            self.currentState = .active;
//            self.layer.shadowColor = UIColor.rsLightGreenSea.withAlphaComponent(1).cgColor;
//            self.layer.shadowRadius  = 4;
//            break;
//        }
//    }
//    
//    
//    func initWithCoder() {
////        super.()
//        if self.isSelected {
//            currentState = .active
//            self.isSelected = false
//        }
//        if (currentState == .active) {
//            self.layer.shadowColor = UIColor.rsLightGreenSea.withAlphaComponent(1).cgColor;
//            self.layer.shadowRadius  = 4;
//        }
//        if (currentState == .ready) {
//            self.layer.shadowColor = UIColor.rsLightGreenSea.withAlphaComponent(0.25).cgColor;
//            self.layer.shadowRadius  = 4;
//        }
//    }
//        
    /*
    // Only override draw() if you perform custom drawing.
    // An empty implementation adversely affects performance during animation.
    override func draw(_ rect: CGRect) {
        // Drawing code
    }
    */

}
